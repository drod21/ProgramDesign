#include <stdio.h>

int main(void)
{

	int *p;
	printf("the value of the pointer: %p", p);
//	printf("the value of the value that the pointer points to: %d", *p);

//	*p = 20;

//	printf("the value of the value that the pointer points to: %d", *p);
	return 0;
}
